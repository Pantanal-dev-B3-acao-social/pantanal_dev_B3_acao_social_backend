spring:
  datasource:
    username: sa
    password: 123
    url: jdbc:h2:mem:testdbacaosocial
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false
    baseline-on-migrate: false
